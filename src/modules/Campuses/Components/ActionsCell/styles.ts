import { styled } from '@mui/material';

export const ContainerButtons = styled('div', { name: 'ContainerButtons' })`
  display: flex;
  flex-wrap: nowrap;
  flex-direction: row;
  column-gap: 4px;
  justify-content: flex-end;
`;
