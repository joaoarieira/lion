import { BoxProps } from '@mui/material';
import { StyledBox } from './styled';

export function FormFooter(props: BoxProps): JSX.Element {
  return <StyledBox {...props} />;
}

export default FormFooter;
